import { defineConfig } from 'dumi';

export default defineConfig({
  outputPath: 'docs-dist',
  base: '/react-rule-tree-v2/',
  publicPath: '/react-rule-tree-v2/',
  themeConfig: {
    name: 'react-rule-tree',
    socialLinks: {
      github: 'https://github.com/xiefengnian/react-rule-tree-v2',
    },
  },
});
